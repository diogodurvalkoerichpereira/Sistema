Sistema de agendamentos.

O Sistema construido com C#, Asp.Net, BoothStrap, HTML e CSS com IDE Visual Studio, ultilizando as Bibliotecas de Class Entity Framework.

O banco de dados em SQL com o Entity.Framework e Habilitando o Migration para manipução do banco de dados.


O sistema resgitra as informações dos usuarios, das salas, e agendamentos todas com vinculos e listagem, tanto para usuarios, tanto
para agendamentos, e salas.


Iniciar a Intalação

1. Para ligar é necessario intalar o banco de dados SQL.
2. Devera nomear com o mesmo nome do projeto o banco de dados "SistemaA" ou com outronome de sua prefencia mas devera nomear as referencias.
3. Configurar o endereço no web.config que aponta para o seu servidor.
4. Após realizar esses 3 processos é só iniciar que o arquivo Global foi configurado para gerar automaticamente as planilhas
em seu banco de dados, mas novamente não esqueça de nomear com o mesmo nome para ser gerado.
5. Senha de acesso para ultilizar o usuario pode ultilizar qualquer uma para se registrar
o sistema não foi ativado a segurança com o super usuario.

Realizando esses procedimentos apontados o sistema funcionara tranquilamente.


